---
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tumbler/tags/{{ tag }}/
---
{% layout 'layout' %}
{% block content %}
<h1>Tagged “{{ tag }}”</h1>

<ul>
  {% for post in collections[tag] | reverse %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
  {% endfor %}
</ul>

<div class="goBack">
  <a href="/tumbler/tags/">← All Tags</a>
</div>

{% endblock %}
