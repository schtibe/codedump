<div class="tagList">
  <ul>
    {% assign taglist = tags | filterTagList %}
    {% for tag in taglist %}
      <li>
        <a href="/tumbler/tags/{{ tag | slugify}}" class="post-tag">{{ tag }}</a>
      </li>
    {% endfor %}
  </ul>
</div>
